require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.
#
# Also compared to earlier versions of this generator, there are no longer any
# expectations of assigns and templates rendered. These features have been
# removed from Rails core in Rails 5, but can be added back in via the
# `rails-controller-testing` gem.

RSpec.describe StudyGradesController, type: :controller do

  # This should return the minimal set of attributes required to create a valid
  # StudyGrade. As you add validations to StudyGrade, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # StudyGradesController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET #index" do
    it "returns a success response" do
      StudyGrade.create! valid_attributes
      get :index, params: {}, session: valid_session
      expect(response).to be_successful
    end
  end

  describe "GET #show" do
    it "returns a success response" do
      study_grade = StudyGrade.create! valid_attributes
      get :show, params: {id: study_grade.to_param}, session: valid_session
      expect(response).to be_successful
    end
  end

  describe "GET #new" do
    it "returns a success response" do
      get :new, params: {}, session: valid_session
      expect(response).to be_successful
    end
  end

  describe "GET #edit" do
    it "returns a success response" do
      study_grade = StudyGrade.create! valid_attributes
      get :edit, params: {id: study_grade.to_param}, session: valid_session
      expect(response).to be_successful
    end
  end

  describe "POST #create" do
    context "with valid params" do
      it "creates a new StudyGrade" do
        expect {
          post :create, params: {study_grade: valid_attributes}, session: valid_session
        }.to change(StudyGrade, :count).by(1)
      end

      it "redirects to the created study_grade" do
        post :create, params: {study_grade: valid_attributes}, session: valid_session
        expect(response).to redirect_to(StudyGrade.last)
      end
    end

    context "with invalid params" do
      it "returns a success response (i.e. to display the 'new' template)" do
        post :create, params: {study_grade: invalid_attributes}, session: valid_session
        expect(response).to be_successful
      end
    end
  end

  describe "PUT #update" do
    context "with valid params" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested study_grade" do
        study_grade = StudyGrade.create! valid_attributes
        put :update, params: {id: study_grade.to_param, study_grade: new_attributes}, session: valid_session
        study_grade.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the study_grade" do
        study_grade = StudyGrade.create! valid_attributes
        put :update, params: {id: study_grade.to_param, study_grade: valid_attributes}, session: valid_session
        expect(response).to redirect_to(study_grade)
      end
    end

    context "with invalid params" do
      it "returns a success response (i.e. to display the 'edit' template)" do
        study_grade = StudyGrade.create! valid_attributes
        put :update, params: {id: study_grade.to_param, study_grade: invalid_attributes}, session: valid_session
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE #destroy" do
    it "destroys the requested study_grade" do
      study_grade = StudyGrade.create! valid_attributes
      expect {
        delete :destroy, params: {id: study_grade.to_param}, session: valid_session
      }.to change(StudyGrade, :count).by(-1)
    end

    it "redirects to the study_grades list" do
      study_grade = StudyGrade.create! valid_attributes
      delete :destroy, params: {id: study_grade.to_param}, session: valid_session
      expect(response).to redirect_to(study_grades_url)
    end
  end

end
